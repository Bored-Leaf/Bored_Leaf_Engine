Only used to test engine, will not be used to make a game.

Benefits:
- If game solution needs a new feature from the engine I have a way to check that the new feature doesn't break existing feature via this GameTest
- Have a readily available demo for people using the engine. Shows how to use the engine.

## Demo
Have a boss fight that demonstrates the capabilities of the engine, even if it looks overboard just show what the engine can do.
Also upload the whole GameDemo so people can play and have a look without having to download the whole engine itself.
To project engine IP:
- Ship only the .dll, not the headers or lib
- Optionally strip or obfuscate or strip sumbols
- 